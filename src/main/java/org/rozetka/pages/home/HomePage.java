package org.rozetka.pages.home;

import org.openqa.selenium.WebDriver;
import org.rozetka.pages.BasePageWithHeader;

public class HomePage extends BasePageWithHeader {

    public HomePage(WebDriver driver) {
        super(driver);
    }
}
